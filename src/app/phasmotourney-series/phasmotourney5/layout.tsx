import React from "react";

export const metadata = { title: "Phasmo Tourney 5" };

export default function Tourney5Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
